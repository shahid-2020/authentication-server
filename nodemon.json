{
  "watch": ["src"],
  "ext": "ts,json",
  "exec": "ts-node -r tsconfig-paths/register --transpile-only src/main.ts",
  "env": {
    "NODE_ENV": "dev",
    "PORT": "3000",
    "DATABASE_URL":"postgresql://Shahid_Islam:@localhost:5432/postgres?schema=public"
  },
  "delay": "250",
  "signal": "SIGTERM",
  "restartable": "rs"
}
